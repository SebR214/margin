# Changelog

The long tail of `ROADMAP.md`. A dated shipped-log entry moves here once it is
**more than 30 days old**, so the roadmap stays short enough that three agents
can read it on every run without drowning in history.

Nothing is rewritten on the way in. An entry is moved verbatim, under the same
heading, so a link into the roadmap's history still finds the same words.

## Nothing has aged out yet

The oldest dated entry in `ROADMAP.md` is **2026-08-18**, which crosses 30 days
on **2026-09-17**. Until then this file is a promise, not a record.
